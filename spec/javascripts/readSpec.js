describe("readGame", function(){
  var game;

  beforeEach(function(){
    game = readGame(createResponsiveCanvas());
  });

it("should create a canvas for letters", function(){
    expect(game).toEqual(game)
  });
});